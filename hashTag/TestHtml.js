// hashChange1 = function () {

//  if (location.hash === "#findReference") {
//    console.log("getting the references please wait...");
//}
//};

//if anonymus only function
window.addEventListener("hashchange", function () {

    if (location.hash === "#findReference") {
        console.log("getting the references please wait...");
    }
});